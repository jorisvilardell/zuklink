# This app uses the global .env file at the project root
# See ../../.env.example for configuration options
#
# When running from the app directory, environment variables
# are loaded from the workspace root .env file
